---
import ClickerPage from "@/components/pages/ClickerPage";
import Layout from "@/layouts/Layout.astro";
import { createPageProps } from "@/lib";

const props = createPageProps();
---

<Layout pageProps={props}>
  <ClickerPage pageProps={props} client:load />
</Layout>
