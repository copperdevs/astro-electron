import { type AppInfo as ApplicationInfo } from "./lib";

export const AppInfo: ApplicationInfo = {
  title: "Untitled Application",
  windowsMaterial: "mica", // requires restart on change
  customTitleBar: true, // requires restart on change
};
